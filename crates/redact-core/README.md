# redact-core

Library for offline PDF redaction: detect PII, rasterize pages, paint redactions,
rebuild with krilla, verify four ways, and emit a hash certificate.

See the workspace `ARCHITECTURE.md` for design details.
